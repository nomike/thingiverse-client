# Config for openapi-python-client (used when generating thingiverse)
project_name_override: thingiverse-client
package_name_override: thingiverse
# Version is taken from pyproject.toml or set by release-please
post_hooks:
  - "ruff check . --fix-only"
  - "ruff format ."
